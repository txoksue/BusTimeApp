Configuration:
  status: warn
  Appenders:
    Lambda:
      name: Lambda
      PatternLayout:
        Pattern: "%d{yyyy-MM-dd HH:mm:ss} %X{AWSRequestId} %-5p %c{1}:%L -  %m%n"
  Loggers:
    Root:
      level: error
      AppenderRef:
        ref: Lambda